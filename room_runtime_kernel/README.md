# Room Runtime Kernel

This folder adds a shared browser runtime above the room-engine specifications.

It provides project selection, instrument execution, local state persistence, acceptance checking, and JSON receipt export.

Claim boundary: this proves a stateful package runtime for project instruments. It does not prove that every underlying external system is production-complete.
