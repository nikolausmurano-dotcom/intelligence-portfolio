# Conversation capture with structured JSON handoff

This folder contains the package-level room-engine slice for `mb-ci-career-conversation-handoff-done`.

It proves named instruments, fixture logic, acceptance tests, export schema, and claim boundary. It does not prove the full external system is complete.
