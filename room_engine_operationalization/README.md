# Room Engine Operationalization

This layer converts project full-build contracts into package-level operational slices.

Each project receives:

- named instruments,
- a first fixture,
- an export schema,
- acceptance tests,
- an execution receipt,
- a browser runner entry.

Claim boundary: this proves package-level operational slices, not full completion of every external software system, corpus service, manuscript program, or research platform.
